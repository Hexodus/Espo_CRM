This is an allmost untouched EspoCRM docker installation.

### Quick Start
Make an own `.env` file with the credentials of your choice.
Run in the console `docker compose -up` and open it in the browser.
